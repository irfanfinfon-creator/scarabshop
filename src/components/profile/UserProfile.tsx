import { UnifiedProfile } from './UnifiedProfile';

/**
 * UserProfile component - Main container for user profile management
 * Now uses UnifiedProfile for a stacked layout
 */
export function UserProfile() {
  return <UnifiedProfile />;
}
